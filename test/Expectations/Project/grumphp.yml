parameters:
  git_dir: .
  bin_dir: vendor/bin
  tasks:
    git_blacklist:
      keywords:
        - "die("
        - "var_dump("
        - "exit;"
    phplint:
    phpcsfixer2:
      config: '.php_cs'
    xmllint:
